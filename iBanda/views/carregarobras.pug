extends layout
block content
  h4 Fazer upload do ficheiro
    .w3-container
    .w3-card-4
      header.w3-teal.w3-container
        h3 SIGN UP
      .w3-container        
        form.w3-container(method="post" enctype="multipart/form-data" action="/obras/carregarFile")
          input.w3-btn.w3-teal(type='file' name='samplefile' accept=".zip")
          button.w3-btn.w3-teal(type='submit' value='Submeter Ficheiro') Submeter Ficheiro